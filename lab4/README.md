## 4.3

There aren't any instances of “xpath”.
Though XPath selectors are very flexible, they are typically not performance tested by browser vendors and tend to be quite slow.
There are css selectors.
